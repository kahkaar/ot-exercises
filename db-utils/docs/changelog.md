# Changelog

## Week 3

### Added

- `UI` class in [`gui.py`](/src/gui.py) with Tkinter
- `DatabaseService` class in [`services/database.py`](/src/services/database.py) to handle database validation and table listing
- Ability to open a local SQLite database file through the GUI
- Ability to view all tables in the local SQLite database
- Created `invoke` tasks in [`tasks.py`](/tasks.py)
- Unit tests for the `DatabaseService` class.
